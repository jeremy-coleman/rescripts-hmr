ts
```ts
// Using Yarn:
yarn add undux

// Or, using NPM:
npm install undux --save
```

flow
```ts
// Using Yarn:
yarn add undux

// Or, using NPM:
npm install undux --save
```

es6
```ts
// Using Yarn:
yarn add undux

// Or, using NPM:
npm install undux --save
```

es5
```ts
// Using Yarn:
yarn add undux

// Or, using NPM:
npm install undux --save
```
